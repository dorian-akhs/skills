{
  "expectations": [
    {
      "text": "The response includes evidence that the coach read repo files before producing output",
      "passed": true,
      "evidence": "Agent made 23 tool calls including reads of AGENTS.md, src/middleware.ts, src/env.ts, src/lib/startup.ts. The non-obvious observation (AGENTS.md invariant #2 not type-enforced) requires reading both AGENTS.md and src/core/ absence."
    },
    {
      "text": "The orient includes at least one non-obvious observation (a risk, sharp edge, implicit invariant, or notable pattern — not just naming directories and files)",
      "passed": true,
      "evidence": "Explicitly labeled 'Non-obvious observation (the sharp edge)': AGENTS.md invariant #2 (only account.address exits src/core/keys/) is enforced nowhere — not in the type system, not in tests, not in ESLint. First implementer of src/core/keys/ could silently leak private key material."
    },
    {
      "text": "The response ends with exactly one question",
      "passed": true,
      "evidence": "Response ends with exactly one question: 'What are the two things it validates during startup, and what happens if either validation fails?'"
    },
    {
      "text": "The question requires genuine recall or reasoning, not a lookup (it cannot be answered by just reading the file name)",
      "passed": true,
      "evidence": "Question requires recalling both (1) migrations and (2) chain ID validation as the two startup checks, plus knowing that process.exit(1) is called on failure. Cannot be answered from filenames alone."
    }
  ],
  "summary": {
    "passed": 4,
    "failed": 0,
    "total": 4,
    "pass_rate": 1.0
  }
}
